(setq default-frame-alist '(
               (width . 201)
               (height . 60) ))

;; bookmarks location
(setq bookmark-default-file "~/.emacs.d/.bookmarks.ideamonk")

;; place file
(setq save-place-file "~/.emacs.d/.saved-places.ideamonk")

